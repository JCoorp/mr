# Carpeta de modelos

Coloca aquí el archivo optimizado:

```txt
casa-mejor.glb
```

La página `app.js` carga el modelo desde:

```txt
models/casa-mejor.glb
```

El archivo GLB original era mayor a 100 MB, por eso conviene usar una versión optimizada antes de subirla a GitHub.
